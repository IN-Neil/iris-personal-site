diff -ruN --exclude=node_modules --exclude=.next --exclude=out --exclude=next-env.d.ts --exclude=tsconfig.tsbuildinfo base/src/app/page.tsx proto/src/app/page.tsx
--- base/src/app/page.tsx	2026-09-13 22:26:33
+++ proto/src/app/page.tsx	2026-09-14 21:50:25
@@ -1,19 +1,13 @@
 import { Soundtrack } from "@/components/journey/Soundtrack";
 import { Journey } from "@/components/journey/Journey";
-import { JourneyStacked } from "@/components/journey/JourneyStacked";
 
+// PROTOTYPE: one layout at every width. Framing is chosen inside Journey
+// (?frame=wide|compact|crop, default by aspect ratio).
 export default function Home() {
   return (
     <main>
       <Soundtrack />
-      {/* Wide screens: scroll-driven side-scrolling journey */}
-      <div className="hidden md:block">
-        <Journey />
-      </div>
-      {/* Small screens: the same story, stacked */}
-      <div className="md:hidden">
-        <JourneyStacked />
-      </div>
+      <Journey />
     </main>
   );
 }
diff -ruN --exclude=node_modules --exclude=.next --exclude=out --exclude=next-env.d.ts --exclude=tsconfig.tsbuildinfo base/src/components/journey/Journey.tsx proto/src/components/journey/Journey.tsx
--- base/src/components/journey/Journey.tsx	2026-09-13 22:26:33
+++ proto/src/components/journey/Journey.tsx	2026-09-14 21:50:25
@@ -5,56 +5,169 @@
 import {
   activeSegment,
   chapterPhases,
+  clamp,
   fadeWindow,
+  keyframes,
   SCROLL_SCREENS,
   storyProgress,
   segmentOrder,
   segments,
   type SegmentKey,
 } from "@/lib/journey";
+import { CROP_ASPECT, cropStops } from "@/lib/crop";
 import { ChapterLabel, ChapterPanel, EndingPanel, IntroPanel } from "./Panels";
 import { Stage } from "./Stage";
 
 /**
- * Desktop layout: one tall scroll container with a sticky, viewport-sized
- * stage inside it. Vertical scroll position becomes `progress` (0→1), which
- * drives the horizontal camera, zoom, time of day, and which panel is shown.
+ * PROTOTYPE. One scroll container with a sticky, large-viewport-sized stage.
+ * Scroll progress = (scrollY − containerTop) / (containerHeight − lvhPx),
+ * with all three sizes cached and re-measured only when the width or the
+ * 100lvh probe changes (not when the toolbar changes innerHeight).
  */
 
-function useScrollProgress(ref: React.RefObject<HTMLElement | null>) {
+type Frame = "wide" | "compact" | "crop";
+
+type Debug = {
+  ignoredResizes: number;
+  remeasures: number;
+  top: number;
+  height: number;
+  lvh: number;
+  width: number;
+  scrollProgress: number;
+};
+
+declare global {
+  interface Window {
+    __journeyDebug?: Debug;
+  }
+}
+
+function useScrollProgress(
+  ref: React.RefObject<HTMLElement | null>,
+  probeRef: React.RefObject<HTMLElement | null>,
+) {
   const [progress, setProgress] = useState(0);
+  const [size, setSize] = useState({ width: 0, lvh: 0 });
 
   useEffect(() => {
     const element = ref.current;
-    if (!element) return;
+    const probe = probeRef.current;
+    if (!element || !probe) return;
 
+    const debug: Debug = { ignoredResizes: 0, remeasures: 0, top: 0, height: 0, lvh: 0, width: 0, scrollProgress: 0 };
+    window.__journeyDebug = debug;
+
+    const measure = () => {
+      const rect = element.getBoundingClientRect();
+      debug.top = rect.top + window.scrollY;
+      debug.height = rect.height;
+      debug.lvh = probe.getBoundingClientRect().height;
+      debug.width = document.documentElement.clientWidth;
+      setSize({ width: debug.width, lvh: debug.lvh });
+    };
+    const read = () => {
+      const scrollable = debug.height - debug.lvh;
+      return scrollable > 0 ? clamp((window.scrollY - debug.top) / scrollable) : 0;
+    };
+
     let frame = 0;
     const update = () => {
       frame = 0;
-      // Hidden on small screens; nothing to measure.
-      if (element.offsetParent === null) return;
-      const rect = element.getBoundingClientRect();
-      const scrollable = rect.height - window.innerHeight;
-      if (scrollable <= 0) return;
-      setProgress(Math.min(1, Math.max(0, -rect.top / scrollable)));
+      debug.scrollProgress = read();
+      setProgress(debug.scrollProgress);
     };
     const schedule = () => {
       if (!frame) frame = requestAnimationFrame(update);
     };
+    const restore = (scrollProgress: number) => {
+      window.scrollTo({ top: debug.top + scrollProgress * (debug.height - debug.lvh), behavior: "instant" });
+      schedule();
+    };
+    const onResize = () => {
+      const width = document.documentElement.clientWidth;
+      const lvh = probe.getBoundingClientRect().height;
+      // Toolbar collapse/expand changes innerHeight only; the 100lvh probe and width stay put.
+      if (width === debug.width && Math.abs(lvh - debug.lvh) < 1) {
+        debug.ignoredResizes++;
+        return;
+      }
+      const keep = debug.scrollProgress;
+      debug.remeasures++;
+      measure();
+      restore(keep);
+    };
 
-    update();
+    measure();
+    const initial = new URLSearchParams(window.location.search).get("s");
+    if (initial !== null) restore(clamp(Number(initial) / (SCROLL_SCREENS - 1)));
+    else update();
+
+    document.fonts?.ready.then(() => {
+      const keep = read();
+      measure();
+      if (initial !== null) restore(clamp(Number(initial) / (SCROLL_SCREENS - 1)));
+      else restore(keep);
+    });
+    const onPageShow = () => {
+      measure();
+      schedule();
+    };
+
     window.addEventListener("scroll", schedule, { passive: true });
-    window.addEventListener("resize", schedule);
+    window.addEventListener("resize", onResize);
+    window.addEventListener("pageshow", onPageShow);
     return () => {
       window.removeEventListener("scroll", schedule);
-      window.removeEventListener("resize", schedule);
+      window.removeEventListener("resize", onResize);
+      window.removeEventListener("pageshow", onPageShow);
       if (frame) cancelAnimationFrame(frame);
     };
-  }, [ref]);
+  }, [ref, probeRef]);
 
-  return progress;
+  return { progress, size };
 }
 
+function useFrame(): Frame {
+  const [frame, setFrame] = useState<Frame>("wide");
+  useEffect(() => {
+    const requested = new URLSearchParams(window.location.search).get("frame");
+    if (requested === "wide" || requested === "compact" || requested === "crop") setFrame(requested);
+    else setFrame(window.matchMedia("(max-aspect-ratio: 9/10)").matches ? "crop" : "wide");
+  }, []);
+  return frame;
+}
+
+/** Debug readout for the iOS Simulator (?hud=1). */
+function Hud() {
+  const ref = useRef<HTMLPreElement>(null);
+  useEffect(() => {
+    const probes = ["svh", "lvh", "dvh"].map((unit) => {
+      const el = document.createElement("div");
+      el.style.cssText = `position:fixed;top:0;left:0;width:0;visibility:hidden;height:100${unit}`;
+      document.body.appendChild(el);
+      return [unit, el] as const;
+    });
+    const id = window.setInterval(() => {
+      const d = window.__journeyDebug;
+      if (!ref.current || !d) return;
+      const units = probes.map(([u, el]) => `${u}:${Math.round(el.getBoundingClientRect().height)}`).join(" ");
+      ref.current.textContent =
+        `inner ${window.innerWidth}×${window.innerHeight} client ${document.documentElement.clientHeight}\n` +
+        `vv ${Math.round(window.visualViewport?.height ?? 0)} ${units}\n` +
+        `scrollY ${Math.round(window.scrollY)} p ${d.scrollProgress.toFixed(4)}\n` +
+        `ignored ${d.ignoredResizes} remeasured ${d.remeasures}`;
+    }, 200);
+    return () => window.clearInterval(id);
+  }, []);
+  return (
+    <pre
+      ref={ref}
+      className="pointer-events-none fixed left-2 top-[40%] z-50 bg-black/80 p-2 font-mono text-[11px] leading-tight text-lime-300"
+    />
+  );
+}
+
 /** Fades a panel in and out and nudges it upward as it settles. */
 function panelStyle(visibility: number): React.CSSProperties {
   return {
@@ -64,11 +177,6 @@
   };
 }
 
-/**
- * Where each chapter's words sit. The scene decides: chapter one's moon is
- * low on the right, so the text takes the upper left; from chapter two the
- * moon and clouds fill the left, so the text moves right.
- */
 const textPlacement: Record<Chapter["id"], { left: string; top: string }> = {
   questions: { left: "9%", top: "20%" },
   building: { left: "50%", top: "13%" },
@@ -76,6 +184,15 @@
   part: { left: "50%", top: "13%" },
 };
 
+/** PROTOTYPE portrait overlay: full-width column; chapter two starts below the moon. */
+const portraitTop: Record<Chapter["id"], string> = {
+  questions: "max(92px, calc(env(safe-area-inset-top) + 76px))",
+  building: "31%",
+  community: "max(92px, calc(env(safe-area-inset-top) + 76px))",
+  part: "max(92px, calc(env(safe-area-inset-top) + 76px))",
+};
+const portraitColumn: React.CSSProperties = { left: 24, right: 24, width: "auto" };
+
 const routeLabels: Record<SegmentKey, string> = {
   intro: "Departure",
   questions: chapters[0].label,
@@ -88,7 +205,7 @@
 function RouteMap({ progress }: { progress: number }) {
   const current = activeSegment(progress);
   const stops: SegmentKey[] = segmentOrder.filter((key) => key !== "intro");
-  const currentIndex = stops.indexOf(current); // -1 while still on the intro
+  const currentIndex = stops.indexOf(current);
 
   return (
     <div className="pointer-events-none absolute bottom-6 left-8 flex items-center gap-4 font-pixel text-[0.75rem] uppercase tracking-[0.12em] text-mist/70">
@@ -129,20 +246,44 @@
 
 export function Journey() {
   const containerRef = useRef<HTMLDivElement>(null);
-  const scrollProgress = useScrollProgress(containerRef);
+  const probeRef = useRef<HTMLDivElement>(null);
+  const frame = useFrame();
+  const [hud, setHud] = useState(false);
+  useEffect(() => setHud(new URLSearchParams(window.location.search).has("hud")), []);
+  const { progress: scrollProgress, size } = useScrollProgress(containerRef, probeRef);
   const progress = storyProgress(scrollProgress);
   const introVisible = fadeWindow(progress, segments.intro, 0.05);
   const endingVisible = fadeWindow(progress, [0.94, 1], 0.035);
 
+  const portrait = frame !== "wide";
+  const crop = frame === "crop";
+  const sceneWidth = crop ? Math.max(size.width, Math.round(size.lvh * CROP_ASPECT)) : size.width;
+  const cropLeft = crop
+    ? clamp((keyframes(progress, cropStops) / 100) * sceneWidth, 0, Math.max(0, sceneWidth - size.width))
+    : 0;
+
   return (
-    <div ref={containerRef} className="relative" style={{ height: `${SCROLL_SCREENS * 100}vh` }}>
-      <div className="sticky top-0 h-screen w-full overflow-hidden">
-        <Stage progress={progress} className="absolute inset-0" />
+    <div
+      ref={containerRef}
+      data-frame={frame}
+      className="relative"
+      style={{ height: `${SCROLL_SCREENS * 100}lvh` }}
+    >
+      <div ref={probeRef} aria-hidden="true" className="pointer-events-none invisible fixed left-0 top-0 w-0" style={{ height: "100lvh" }} />
+      {hud && <Hud />}
+      <div className="sticky top-0 w-full overflow-hidden" style={{ height: "100lvh" }}>
+        <Stage
+          progress={progress}
+          compact={frame === "compact"}
+          crop={crop}
+          className="absolute inset-0"
+          style={crop && sceneWidth ? { width: sceneWidth, transform: `translate3d(${-cropLeft}px, 0, 0)` } : undefined}
+        />
 
-        {/* Title, directly on the sky */}
         <div
+          data-probe="intro"
           className="absolute left-[11%] top-[max(6rem,10%)] w-[min(30rem,44vw)] transition-none"
-          style={panelStyle(introVisible)}
+          style={{ ...panelStyle(introVisible), ...(portrait ? { ...portraitColumn, top: 92 } : {}) }}
         >
           <IntroPanel />
         </div>
@@ -154,13 +295,17 @@
           <span aria-hidden="true" className="scroll-hint block h-5 w-px bg-ivory/70" />
         </div>
 
-        {/* Chapters: the words arrive last, placed wherever the scene has room; the body types itself */}
         {chapters.map((chapter) => {
           const phases = chapterPhases(progress, segments[chapter.id]);
           const place = textPlacement[chapter.id];
           return (
             <div key={chapter.id} className="pointer-events-none absolute inset-0" style={panelStyle(phases.visible)}>
-              <div className="absolute w-[min(27rem,42vw)]" style={{ left: place.left, top: place.top }}>
+              <div
+                data-probe="chapter"
+                data-id={chapter.id}
+                className="absolute w-[min(27rem,42vw)]"
+                style={portrait ? { ...portraitColumn, top: portraitTop[chapter.id] } : { left: place.left, top: place.top }}
+              >
                 <ChapterLabel chapter={chapter} />
                 <ChapterPanel
                   chapter={chapter}
@@ -174,15 +319,14 @@
           );
         })}
 
-        {/* Ending */}
         <div
+          data-probe="ending"
           className="absolute left-[8%] top-[14%] w-[min(30rem,38vw)]"
-          style={panelStyle(endingVisible)}
+          style={{ ...panelStyle(endingVisible), ...(portrait ? { ...portraitColumn, top: 92 } : {}) }}
         >
           <EndingPanel />
         </div>
 
-        {/* Small brand mark once the title has scrolled away */}
         <p
           className="pointer-events-none absolute right-8 top-6 font-pixel text-[0.75rem] uppercase tracking-[0.12em] text-mist/70"
           style={{ opacity: 1 - introVisible }}
diff -ruN --exclude=node_modules --exclude=.next --exclude=out --exclude=next-env.d.ts --exclude=tsconfig.tsbuildinfo base/src/components/journey/Stage.tsx proto/src/components/journey/Stage.tsx
--- base/src/components/journey/Stage.tsx	2026-09-13 22:26:33
+++ proto/src/components/journey/Stage.tsx	2026-09-14 21:50:25
@@ -157,11 +157,13 @@
   progress: number;
   /** Larger sprites for small, stacked scenes on mobile. */
   compact?: boolean;
+  /** PROTOTYPE: phone crop framing; examples use one readable slot. */
+  crop?: boolean;
   className?: string;
   style?: CSSProperties;
 };
 
-export function Stage({ progress, compact = false, className, style }: StageProps) {
+export function Stage({ progress, compact = false, crop = false, className, style }: StageProps) {
   const s = sceneState(progress);
   const boatX = keyframes(s.progress, boatXStops);
   const boatWidth = compact ? 23 : 12;
@@ -194,6 +196,7 @@
 
       {/* The moon: small and distant at departure, rising and swelling into chapter two */}
       <div
+        data-probe="moon"
         className="absolute -translate-x-1/2 -translate-y-1/2"
         style={{
           left: `${moonX}%`,
@@ -322,6 +325,7 @@
         </div>
         <div
           className="absolute -translate-y-full"
+          data-probe="lighthouse"
           style={{ left: `${worldX(1, 0.74, FAR)}%`, opacity: fadeWindow(s.progress, [0.93, 1], 0.04), top: `${HORIZON - 3}%`, width: compact ? "13cqh" : "9.5cqh" }}
         >
           <div
@@ -378,13 +382,13 @@
             width: layerSize(100),
           }}
         >
-          <div className="absolute" style={{ left: compact ? "-26%" : "-10%", top: "76%", width: compact ? "70%" : "40%" }}>
+          <div data-probe="dock" className="absolute" style={{ left: compact ? "-26%" : "-10%", top: "76%", width: compact ? "70%" : "40%" }}>
             <Pixel name="dockWide" priority />
           </div>
-          <div className="absolute -translate-y-full" style={{ left: "11%", top: "79%", width: compact ? "8.3cqh" : "10.3cqh" }}>
+          <div data-probe="adult" className="absolute -translate-y-full" style={{ left: "11%", top: "79%", width: compact ? "8.3cqh" : "10.3cqh" }}>
             <Pixel name="adult" priority />
           </div>
-          <div className="absolute -translate-y-full" style={{ left: compact ? "29%" : "22%", top: "79%", width: compact ? "8cqh" : "10.7cqh" }}>
+          <div data-probe="child" className="absolute -translate-y-full" style={{ left: compact ? "29%" : "22%", top: "79%", width: compact ? "8cqh" : "10.7cqh" }}>
             <Pixel name="child" priority />
           </div>
         </div>
@@ -397,11 +401,13 @@
           {markers.map((marker, i) => (
             <div
               key={`${marker.id}-${i}`}
+              data-probe="marker"
+              data-text={marker.text}
               className="absolute flex items-start gap-3 font-pixel text-[clamp(1.1rem,1.7vw,1.65rem)] leading-snug text-mist"
               style={{
-                left: `${worldX(marker.at, marker.vx, MID)}%`,
+                left: `${worldX(marker.at, crop ? 0.19 : marker.vx, MID)}%`,
                 top: `${marker.top}%`,
-                width: layerSize(32),
+                width: layerSize(crop ? 31 : 32),
                 opacity: exampleVisibility(s.progress, marker.window),
               }}
             >
@@ -414,6 +420,7 @@
 
       {/* The paper boat is already afloat beyond the dock. */}
       <div
+        data-probe="boat"
         className="absolute -translate-y-full"
         style={{
           left: `${boatX}%`,
diff -ruN --exclude=node_modules --exclude=.next --exclude=out --exclude=next-env.d.ts --exclude=tsconfig.tsbuildinfo base/src/lib/crop.ts proto/src/lib/crop.ts
--- base/src/lib/crop.ts	1969-12-31 19:00:00
+++ proto/src/lib/crop.ts	2026-09-14 21:50:25
@@ -0,0 +1,18 @@
+import type { Stop } from "@/lib/journey";
+
+/**
+ * PROTOTYPE. Phones only: left edge of the visible window, as % of the
+ * wider scene (scene width = max(viewport width, 0.9 × large viewport height)).
+ * These are the values tested in the earlier browser patch, unchanged.
+ */
+export const cropStops: Stop[] = [
+  [0, 6],
+  [0.12, 6],
+  [0.19, 14],
+  [0.84, 14],
+  [0.95, 40],
+  [1, 41],
+];
+
+/** Scene width in the crop framing (desktop-like aspect). */
+export const CROP_ASPECT = 0.9;
